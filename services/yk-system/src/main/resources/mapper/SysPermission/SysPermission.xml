<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.yk.authentication.dao.SysPermissionDao">

    <select id="selectPermissionByUserName" parameterType="String" resultType="map">
        SELECT
        p.*
        FROM
        sys_permission p
        LEFT JOIN sys_role_permission rp ON p.permission_guid = rp.permission_guid
        INNER JOIN sys_user_role ur ON rp.role_guid = ur.role_guid
        <if test="userName != null">
            WHERE
            ur.user_guid = ( SELECT user_guid FROM sys_user u WHERE u.user_name = #{userName} )
        </if>
    </select>

</mapper>
